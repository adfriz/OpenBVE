using System;
using Silk.NET.OpenGL.Legacy;

public class Program
{
    public static void Main()
    {
        Console.WriteLine(typeof(GLEnum).FullName);
        Console.WriteLine(typeof(GL).FullName);
    }
}
